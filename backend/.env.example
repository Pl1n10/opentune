# dsc-cp Backend Configuration
# Copy this file to .env and customize the values

# =============================================================================
# Application Settings
# =============================================================================

# Project name (shown in Swagger UI)
PROJECT_NAME=dsc-cp

# API version prefix
API_V1_STR=/api/v1

# Enable debug mode (logs SQL queries, etc.)
DEBUG=false

# =============================================================================
# Database
# =============================================================================

# SQLite (default, good for small deployments)
DATABASE_URL=sqlite:///./dsc_cp.db

# PostgreSQL (recommended for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/dsc_cp

# =============================================================================
# Security
# =============================================================================

# Admin API Key for management endpoints
# IMPORTANT: Generate a secure key for production!
# Run: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_API_KEY=CHANGE-ME-GENERATE-A-SECURE-KEY

# Node token length in bytes (default: 32, results in ~43 char tokens)
NODE_TOKEN_BYTES=32
